There are two options to compile the code:

Option 1.  Use the Makefile in the current directory by running the command in the terminal:
    make

OR 

Option 2.  Manually compile by typing:
    gcc -std=gnu99 -Wall -o smallsh main.c

Then run the program by typing:
    ./smallsh